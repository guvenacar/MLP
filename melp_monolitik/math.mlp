-- Math Module (math.mlp)
module Math
    export func add(numeric a, numeric b)
        return a + b
    end func
    
    export func multiply(numeric x, numeric y)
        return x * y
    end func
end module
