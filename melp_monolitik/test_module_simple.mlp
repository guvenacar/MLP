-- Simple module definition for testing
module MathUtils
    func add(numeric a, numeric b)
        return a + b
    end func
    
    func multiply(numeric x, numeric y)
        return x * y
    end func
end module
