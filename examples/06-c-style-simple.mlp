// syntax: c-style
// lang: tr-TR

PROGRAM test_c_style {
    sayisal x = 42;
    sayisal y = 10;
    
    eğer (x > y) {
        yazdir("x büyük");
    } değilse {
        yazdir("y büyük");
    }
    
    döngü (y < 15) {
        yazdir(y);
        y = y + 1;
    }
}
