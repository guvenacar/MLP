# MLP Parser

The parser is the second stage of the MLP compiler. It converts the token stream into an Abstract Syntax Tree (AST).

## Responsibilities

- Build AST from tokens
- Enforce syntax rules
- Report syntax errors
- Handle operator precedence
- Support multi-syntax styles

## Implementation Status

🚧 **Under Development** - Will be implemented in MLP itself during bootstrap phase.
