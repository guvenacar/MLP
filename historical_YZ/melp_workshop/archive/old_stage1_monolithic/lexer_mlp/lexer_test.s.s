# Main program: lexer.mlp
# Generated by MELP Compiler (YZ_44 Part 5.1)

.section .rodata
.div_zero_msg:
    .string "Division by zero is not allowed!"

.text


# Function: create_token
.global create_token
create_token:
    pushq %rbp
    movq %rsp, %rbp
    subq $32, %rsp
    # Parameter: token_type at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: token_value at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Parameter: line_num at -24(%rbp)
    movq %rdx, -24(%rbp)
    # Parameter: col_num at -32(%rbp)
    movq %rcx, -32(%rbp)

    # Arithmetic expression
    # Collection literal
    # Array literal: 4 elements, type=0
    # Allocate array on heap
    movq $4, %rdi  # Number of elements
    movq $8, %rsi   # Element size (8 bytes)
    call sto_array_alloc  # Returns pointer in rax
    movq %rax, %rbx  # Save array pointer in rbx
    # Initialize array elements
    # Element 0
    pushq %rbx

    # Arithmetic expression
    movq -8(%rbp), %r8  # Load token_type
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 0(%rbx)  # Store element at index 0
    # Element 1
    pushq %rbx

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load token_value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 8(%rbx)  # Store element at index 1
    # Element 2
    pushq %rbx

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load line_num
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 16(%rbx)  # Store element at index 2
    # Element 3
    pushq %rbx

    # Arithmetic expression
    movq -32(%rbp), %r8  # Load col_num
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 24(%rbx)  # Store element at index 3
    movq %rbx, %rax  # Array pointer result
    movq %rax, %r8  # Collection pointer
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lcreate_token_return

.Lcreate_token_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: create_position_result
.global create_position_result
create_position_result:
    pushq %rbp
    movq %rsp, %rbp
    subq $24, %rsp
    # Parameter: p at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: c at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Parameter: l at -24(%rbp)
    movq %rdx, -24(%rbp)

    # Arithmetic expression
    # Collection literal
    # Array literal: 3 elements, type=0
    # Allocate array on heap
    movq $3, %rdi  # Number of elements
    movq $8, %rsi   # Element size (8 bytes)
    call sto_array_alloc  # Returns pointer in rax
    movq %rax, %rbx  # Save array pointer in rbx
    # Initialize array elements
    # Element 0
    pushq %rbx

    # Arithmetic expression
    movq -8(%rbp), %r8  # Load p
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 0(%rbx)  # Store element at index 0
    # Element 1
    pushq %rbx

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load c
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 8(%rbx)  # Store element at index 1
    # Element 2
    pushq %rbx

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load l
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 16(%rbx)  # Store element at index 2
    movq %rbx, %rax  # Array pointer result
    movq %rax, %r8  # Collection pointer
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lcreate_position_result_return

.Lcreate_position_result_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: is_digit
.global is_digit
is_digit:
    pushq %rbp
    movq %rsp, %rbp
    subq $8, %rsp
    # Parameter: ch at -8(%rbp)
    movq %rdi, -8(%rbp)

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_0:
    .string "0"
.text
    leaq .str_cmp_0(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_1
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_1:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_1:
    .string "1"
.text
    leaq .str_cmp_1(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_3
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_3:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_2:
    .string "2"
.text
    leaq .str_cmp_2(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_5
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_5:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_3:
    .string "3"
.text
    leaq .str_cmp_3(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_7
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_7:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_4:
    .string "4"
.text
    leaq .str_cmp_4(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_9
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_9:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_5:
    .string "5"
.text
    leaq .str_cmp_5(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_11
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_11:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_6:
    .string "6"
.text
    leaq .str_cmp_6(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_13
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_13:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_7:
    .string "7"
.text
    leaq .str_cmp_7(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_15
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_15:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_8:
    .string "8"
.text
    leaq .str_cmp_8(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_17
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_17:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_9:
    .string "9"
.text
    leaq .str_cmp_9(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_19
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return
.if_end_19:

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_digit_return

.Lis_digit_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: is_alpha
.global is_alpha
is_alpha:
    pushq %rbp
    movq %rsp, %rbp
    subq $8, %rsp
    # Parameter: ch at -8(%rbp)
    movq %rdi, -8(%rbp)

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_10:
    .string "a"
.text
    leaq .str_cmp_10(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_21
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_21:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_11:
    .string "b"
.text
    leaq .str_cmp_11(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_23
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_23:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_12:
    .string "c"
.text
    leaq .str_cmp_12(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_25
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_25:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_13:
    .string "d"
.text
    leaq .str_cmp_13(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_27
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_27:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_14:
    .string "e"
.text
    leaq .str_cmp_14(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_29
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_29:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_15:
    .string "f"
.text
    leaq .str_cmp_15(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_31
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_31:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_16:
    .string "g"
.text
    leaq .str_cmp_16(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_33
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_33:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_17:
    .string "h"
.text
    leaq .str_cmp_17(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_35
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_35:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_18:
    .string "i"
.text
    leaq .str_cmp_18(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_37
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_37:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_19:
    .string "j"
.text
    leaq .str_cmp_19(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_39
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_39:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_20:
    .string "k"
.text
    leaq .str_cmp_20(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_41
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_41:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_21:
    .string "l"
.text
    leaq .str_cmp_21(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_43
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_43:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_22:
    .string "m"
.text
    leaq .str_cmp_22(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_45
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_45:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_23:
    .string "n"
.text
    leaq .str_cmp_23(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_47
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_47:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_24:
    .string "o"
.text
    leaq .str_cmp_24(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_49
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_49:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_25:
    .string "p"
.text
    leaq .str_cmp_25(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_51
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_51:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_26:
    .string "q"
.text
    leaq .str_cmp_26(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_53
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_53:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_27:
    .string "r"
.text
    leaq .str_cmp_27(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_55
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_55:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_28:
    .string "s"
.text
    leaq .str_cmp_28(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_57
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_57:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_29:
    .string "t"
.text
    leaq .str_cmp_29(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_59
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_59:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_30:
    .string "u"
.text
    leaq .str_cmp_30(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_61
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_61:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_31:
    .string "v"
.text
    leaq .str_cmp_31(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_63
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_63:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_32:
    .string "w"
.text
    leaq .str_cmp_32(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_65
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_65:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_33:
    .string "x"
.text
    leaq .str_cmp_33(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_67
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_67:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_34:
    .string "y"
.text
    leaq .str_cmp_34(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_69
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_69:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_35:
    .string "z"
.text
    leaq .str_cmp_35(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_71
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_71:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_36:
    .string "A"
.text
    leaq .str_cmp_36(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_73
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_73:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_37:
    .string "B"
.text
    leaq .str_cmp_37(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_75
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_75:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_38:
    .string "C"
.text
    leaq .str_cmp_38(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_77
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_77:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_39:
    .string "D"
.text
    leaq .str_cmp_39(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_79
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_79:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_40:
    .string "E"
.text
    leaq .str_cmp_40(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_81
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_81:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_41:
    .string "F"
.text
    leaq .str_cmp_41(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_83
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_83:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_42:
    .string "G"
.text
    leaq .str_cmp_42(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_85
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_85:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_43:
    .string "H"
.text
    leaq .str_cmp_43(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_87
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_87:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_44:
    .string "I"
.text
    leaq .str_cmp_44(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_89
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_89:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_45:
    .string "J"
.text
    leaq .str_cmp_45(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_91
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_91:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_46:
    .string "K"
.text
    leaq .str_cmp_46(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_93
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_93:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_47:
    .string "L"
.text
    leaq .str_cmp_47(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_95
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_95:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_48:
    .string "M"
.text
    leaq .str_cmp_48(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_97
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_97:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_49:
    .string "N"
.text
    leaq .str_cmp_49(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_99
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_99:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_50:
    .string "O"
.text
    leaq .str_cmp_50(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_101
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_101:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_51:
    .string "P"
.text
    leaq .str_cmp_51(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_103
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_103:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_52:
    .string "Q"
.text
    leaq .str_cmp_52(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_105
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_105:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_53:
    .string "R"
.text
    leaq .str_cmp_53(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_107
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_107:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_54:
    .string "S"
.text
    leaq .str_cmp_54(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_109
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_109:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_55:
    .string "T"
.text
    leaq .str_cmp_55(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_111
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_111:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_56:
    .string "U"
.text
    leaq .str_cmp_56(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_113
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_113:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_57:
    .string "V"
.text
    leaq .str_cmp_57(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_115
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_115:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_58:
    .string "W"
.text
    leaq .str_cmp_58(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_117
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_117:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_59:
    .string "X"
.text
    leaq .str_cmp_59(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_119
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_119:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_60:
    .string "Y"
.text
    leaq .str_cmp_60(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_121
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_121:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_61:
    .string "Z"
.text
    leaq .str_cmp_61(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_123
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return
.if_end_123:

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_alpha_return

.Lis_alpha_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: is_whitespace
.global is_whitespace
is_whitespace:
    pushq %rbp
    movq %rsp, %rbp
    subq $8, %rsp
    # Parameter: ch at -8(%rbp)
    movq %rdi, -8(%rbp)

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_62:
    .string " "
.text
    leaq .str_cmp_62(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_125
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_whitespace_return
.if_end_125:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_63:
    .string "\t"
.text
    leaq .str_cmp_63(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_127
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_whitespace_return
.if_end_127:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_64:
    .string "\n"
.text
    leaq .str_cmp_64(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_129
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_whitespace_return
.if_end_129:

    # If statement

    # Comparison with logical chaining
    movq -8(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_65:
    .string "\r"
.text
    leaq .str_cmp_65(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_131
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_whitespace_return
.if_end_131:

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lis_whitespace_return

.Lis_whitespace_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: skip_whitespace
.global skip_whitespace
skip_whitespace:
    pushq %rbp
    movq %rsp, %rbp
    subq $56, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Parameter: line at -24(%rbp)
    movq %rdx, -24(%rbp)
    # Parameter: col at -32(%rbp)
    movq %rcx, -32(%rbp)
    # Variable: source_len at -40(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -40(%rbp)  # Initialize source_len
    # Variable: ch at -48(%rbp)

.section .rodata
.str_0:
    .string ""
.text
    leaq .str_0(%rip), %r8  # Load string address
    movq %r8, -48(%rbp)  # Initialize ch
    # Variable: ch_is_whitespace at -56(%rbp)
    movq $0, %r8  # Boolean literal: 0
    movq %r8, -56(%rbp)  # Initialize ch_is_whitespace

    # While loop
.while_start_132:

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -40(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setl %al
    # Single comparison result in rax
    test %rax, %rax
    jz .loop_end_132
    # Loop body
    # Assignment: ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -48(%rbp)  # Store to ch
    # Assignment: ch_is_whitespace = ...

    # Arithmetic expression
    # Function call: is_whitespace()
    movq -48(%rbp), %r10  # Load ch
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call is_whitespace
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -56(%rbp)  # Store to ch_is_whitespace

    # If statement

    # Comparison with logical chaining
    movq -56(%rbp), %r8  # Load ch_is_whitespace
    movq $1, %r9  # Literal
    cmpq %r9, %r8
    movq $0, %rax
    sete %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_else_133
    # Then body

    # If statement

    # Comparison with logical chaining
    movq -48(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_66:
    .string "\n"
.text
    leaq .str_cmp_66(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_else_135
    # Then body
    # Assignment: line = ...

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load line
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_0
    jmp .no_overflow_0
.overflow_detected_0:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_0:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Store to line
    # Assignment: col = ...

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
    # Assignment: pos = ...

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_1
    jmp .no_overflow_1
.overflow_detected_1:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_1:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
    jmp .if_end_136
.if_else_135:
    # Else body
    # Assignment: col = ...

    # Arithmetic expression
    movq -32(%rbp), %r8  # Load col
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_2
    jmp .no_overflow_2
.overflow_detected_2:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_2:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
    # Assignment: pos = ...

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_3
    jmp .no_overflow_3
.overflow_detected_3:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_3:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
.if_end_136:
    jmp .if_end_134
.if_else_133:
    # Else body
    # Exit - jump to loop end
    jmp .loop_end_132
.if_end_134:
    jmp .while_start_132
.loop_end_132:

    # Arithmetic expression
    # Function call: create_position_result()
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 1
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_position_result
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lskip_whitespace_return

.Lskip_whitespace_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: skip_line_comment
.global skip_line_comment
skip_line_comment:
    pushq %rbp
    movq %rsp, %rbp
    subq $48, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Parameter: line at -24(%rbp)
    movq %rdx, -24(%rbp)
    # Parameter: col at -32(%rbp)
    movq %rcx, -32(%rbp)
    # Variable: source_len at -40(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -40(%rbp)  # Initialize source_len
    # Variable: ch at -48(%rbp)

.section .rodata
.str_1:
    .string ""
.text
    leaq .str_1(%rip), %r8  # Load string address
    movq %r8, -48(%rbp)  # Initialize ch

    # While loop
.while_start_137:

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -40(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setl %al
    # Single comparison result in rax
    test %rax, %rax
    jz .loop_end_137
    # Loop body
    # Assignment: ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -48(%rbp)  # Store to ch

    # If statement

    # Comparison with logical chaining
    movq -48(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_67:
    .string "\n"
.text
    leaq .str_cmp_67(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_else_138
    # Then body
    # Assignment: pos = ...

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_4
    jmp .no_overflow_4
.overflow_detected_4:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_4:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
    # Assignment: line = ...

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load line
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_5
    jmp .no_overflow_5
.overflow_detected_5:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_5:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Store to line
    # Assignment: col = ...

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
    # Exit - jump to loop end
    jmp .loop_end_137
    jmp .if_end_139
.if_else_138:
    # Else body
    # Assignment: pos = ...

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_6
    jmp .no_overflow_6
.overflow_detected_6:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_6:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
    # Assignment: col = ...

    # Arithmetic expression
    movq -32(%rbp), %r8  # Load col
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_7
    jmp .no_overflow_7
.overflow_detected_7:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_7:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
.if_end_139:
    jmp .while_start_137
.loop_end_137:

    # Arithmetic expression
    # Function call: create_position_result()
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 1
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_position_result
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lskip_line_comment_return

.Lskip_line_comment_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: starts_with_quote
.global starts_with_quote
starts_with_quote:
    pushq %rbp
    movq %rsp, %rbp
    subq $40, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Variable: ch at -24(%rbp)

.section .rodata
.str_2:
    .string ""
.text
    leaq .str_2(%rip), %r8  # Load string address
    movq %r8, -24(%rbp)  # Initialize ch
    # Variable: source_len at -32(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Initialize source_len
    # Variable: quote_code at -40(%rbp)
    movq $34, %r8  # Literal value
    movq %r8, -40(%rbp)  # Initialize quote_code

    # If statement

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -32(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setl %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_141
    # Then body
    # Assignment: ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Store to ch

    # If statement

    # Comparison with logical chaining
    movq -24(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_68:
    .string "a"
.text
    leaq .str_cmp_68(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_143
    # Then body

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return
.if_end_143:

    # If statement

    # Comparison with logical chaining
    movq -24(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_69:
    .string "0"
.text
    leaq .str_cmp_69(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_145
    # Then body

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return
.if_end_145:

    # If statement

    # Comparison with logical chaining
    movq -24(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_70:
    .string " "
.text
    leaq .str_cmp_70(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_147
    # Then body

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return
.if_end_147:

    # If statement

    # Comparison with logical chaining
    movq -24(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_71:
    .string "+"
.text
    leaq .str_cmp_71(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_149
    # Then body

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return
.if_end_149:

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return
.if_end_141:

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_quote_return

.Lstarts_with_quote_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: starts_with_dash_dash
.global starts_with_dash_dash
starts_with_dash_dash:
    pushq %rbp
    movq %rsp, %rbp
    subq $48, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Variable: source_len at -24(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Initialize source_len
    # Variable: next_pos at -32(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -32(%rbp)  # Initialize next_pos
    # Variable: ch at -40(%rbp)

.section .rodata
.str_3:
    .string ""
.text
    leaq .str_3(%rip), %r8  # Load string address
    movq %r8, -40(%rbp)  # Initialize ch
    # Variable: next_ch at -48(%rbp)

.section .rodata
.str_4:
    .string ""
.text
    leaq .str_4(%rip), %r8  # Load string address
    movq %r8, -48(%rbp)  # Initialize next_ch

    # If statement

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -24(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setl %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_151
    # Then body
    # Assignment: ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -40(%rbp)  # Store to ch

    # If statement

    # Comparison with logical chaining
    movq -40(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_72:
    .string "-"
.text
    leaq .str_cmp_72(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_153
    # Then body
    # Assignment: next_pos = ...

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_8
    jmp .no_overflow_8
.overflow_detected_8:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_8:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to next_pos

    # If statement

    # Comparison with logical chaining
    movq -32(%rbp), %r8  # Load next_pos
    movq -24(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setl %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_155
    # Then body
    # Assignment: next_ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -32(%rbp), %r10  # Load next_pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -48(%rbp)  # Store to next_ch

    # If statement

    # Comparison with logical chaining
    movq -48(%rbp), %r8  # Load next_ch

.section .rodata
.str_cmp_73:
    .string "-"
.text
    leaq .str_cmp_73(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_157
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_dash_dash_return
.if_end_157:
.if_end_155:
.if_end_153:
.if_end_151:

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lstarts_with_dash_dash_return

.Lstarts_with_dash_dash_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: peek_next_token_type
.global peek_next_token_type
peek_next_token_type:
    pushq %rbp
    movq %rsp, %rbp
    subq $56, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Variable: source_len at -24(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Initialize source_len
    # Variable: ch at -32(%rbp)

.section .rodata
.str_5:
    .string ""
.text
    leaq .str_5(%rip), %r8  # Load string address
    movq %r8, -32(%rbp)  # Initialize ch
    # Variable: ch_is_digit at -40(%rbp)
    movq $0, %r8  # Boolean literal: 0
    movq %r8, -40(%rbp)  # Initialize ch_is_digit
    # Variable: ch_is_alpha at -48(%rbp)
    movq $0, %r8  # Boolean literal: 0
    movq %r8, -48(%rbp)  # Initialize ch_is_alpha
    # Variable: is_comment at -56(%rbp)
    movq $0, %r8  # Boolean literal: 0
    movq %r8, -56(%rbp)  # Initialize is_comment

    # If statement

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -24(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setge %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_159
    # Then body

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return
.if_end_159:
    # Assignment: ch = ...

    # Arithmetic expression
    # Function call: substring()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq $1, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call mlp_string_substring
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to ch
    # Assignment: is_comment = ...

    # Arithmetic expression
    # Function call: starts_with_dash_dash()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call starts_with_dash_dash
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -56(%rbp)  # Store to is_comment

    # If statement

    # Comparison with logical chaining
    movq -56(%rbp), %r8  # Load is_comment
    movq $1, %r9  # Literal
    cmpq %r9, %r8
    movq $0, %rax
    sete %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_161
    # Then body

    # Arithmetic expression
    movq $5, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return
.if_end_161:
    # Assignment: ch_is_digit = ...

    # Arithmetic expression
    # Function call: is_digit()
    movq -32(%rbp), %r10  # Load ch
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call is_digit
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -40(%rbp)  # Store to ch_is_digit

    # If statement

    # Comparison with logical chaining
    movq -40(%rbp), %r8  # Load ch_is_digit
    movq $1, %r9  # Literal
    cmpq %r9, %r8
    movq $0, %rax
    sete %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_163
    # Then body

    # Arithmetic expression
    movq $1, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return
.if_end_163:
    # Assignment: ch_is_alpha = ...

    # Arithmetic expression
    # Function call: is_alpha()
    movq -32(%rbp), %r10  # Load ch
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call is_alpha
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -48(%rbp)  # Store to ch_is_alpha

    # If statement

    # Comparison with logical chaining
    movq -48(%rbp), %r8  # Load ch_is_alpha
    movq $1, %r9  # Literal
    cmpq %r9, %r8
    movq $0, %rax
    sete %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_165
    # Then body

    # Arithmetic expression
    movq $3, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return
.if_end_165:

    # If statement

    # Comparison with logical chaining
    movq -32(%rbp), %r8  # Load ch

.section .rodata
.str_cmp_74:
    .string "_"
.text
    leaq .str_cmp_74(%rip), %r9  # Load string address
    # String comparison (text vs text)
    movq %r8, %rdi  # arg1: first string
    movq %r9, %rsi  # arg2: second string
    call mlp_string_compare  # Returns <0, 0, or >0 (int/32-bit)
    cmpl $0, %eax  # Compare result with 0 (YZ_69: use eax for int)
    sete %al
    movzbl %al, %eax        # Zero-extend al to eax (YZ_69)
    movslq %eax, %rax       # Sign-extend eax to rax
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_167
    # Then body

    # Arithmetic expression
    movq $3, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return
.if_end_167:

    # Arithmetic expression
    movq $4, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lpeek_next_token_type_return

.Lpeek_next_token_type_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: tokenize_next
.global tokenize_next
tokenize_next:
    pushq %rbp
    movq %rsp, %rbp
    subq $88, %rsp
    # Parameter: source at -8(%rbp)
    movq %rdi, -8(%rbp)
    # Parameter: pos at -16(%rbp)
    movq %rsi, -16(%rbp)
    # Parameter: line at -24(%rbp)
    movq %rdx, -24(%rbp)
    # Parameter: col at -32(%rbp)
    movq %rcx, -32(%rbp)
    # Variable: source_len at -40(%rbp)

    # Arithmetic expression
    # Function call: length()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    popq %rdi  # Restore arg 1
    call mlp_string_length
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -40(%rbp)  # Initialize source_len
    # Variable: new_pos at -48(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -48(%rbp)  # Initialize new_pos
    # Variable: new_col at -56(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -56(%rbp)  # Initialize new_col
    # Variable: new_line at -64(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -64(%rbp)  # Initialize new_line
    # Variable: ws_result at -72(%rbp)

    # Arithmetic expression
    # Function call: create_position_result()
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 1
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 2
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 3
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_position_result
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -72(%rbp)  # Initialize ws_result
    # Variable: token at -80(%rbp)

    # Arithmetic expression
    # Function call: create_token()
    movq $80, %r10  # Literal
    pushq %r10  # Save arg 1
    .section .rodata
    _str_0: .asciz ""
    .section .text
    leaq _str_0(%rip), %r10  # Load string pointer
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_token
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -80(%rbp)  # Initialize token
    # Variable: token_hint at -88(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -88(%rbp)  # Initialize token_hint
    # Assignment: ws_result = ...

    # Arithmetic expression
    # Function call: skip_whitespace()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call skip_whitespace
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -72(%rbp)  # Store to ws_result
    # Assignment: pos = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 0(%rbx), %rax  # Get element at index 0
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
    # Assignment: col = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 8(%rbx), %rax  # Get element at index 1
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
    # Assignment: line = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 16(%rbx), %rax  # Get element at index 2
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Store to line

    # If statement

    # Comparison with logical chaining
    movq -16(%rbp), %r8  # Load pos
    movq -40(%rbp), %r9  # Load source_len
    cmpq %r9, %r8
    movq $0, %rax
    setge %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_169
    # Then body
    # Assignment: token = ...

    # Arithmetic expression
    # Function call: create_token()
    movq $80, %r10  # Literal
    pushq %r10  # Save arg 1
    .section .rodata
    _str_1: .asciz ""
    .section .text
    leaq _str_1(%rip), %r10  # Load string pointer
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_token
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -80(%rbp)  # Store to token

    # Arithmetic expression
    # Collection literal
    # Array literal: 4 elements, type=0
    # Allocate array on heap
    movq $4, %rdi  # Number of elements
    movq $8, %rsi   # Element size (8 bytes)
    call sto_array_alloc  # Returns pointer in rax
    movq %rax, %rbx  # Save array pointer in rbx
    # Initialize array elements
    # Element 0
    pushq %rbx

    # Arithmetic expression
    movq -80(%rbp), %r8  # Load token
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 0(%rbx)  # Store element at index 0
    # Element 1
    pushq %rbx

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 8(%rbx)  # Store element at index 1
    # Element 2
    pushq %rbx

    # Arithmetic expression
    movq -32(%rbp), %r8  # Load col
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 16(%rbx)  # Store element at index 2
    # Element 3
    pushq %rbx

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load line
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 24(%rbx)  # Store element at index 3
    movq %rbx, %rax  # Array pointer result
    movq %rax, %r8  # Collection pointer
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Ltokenize_next_return
.if_end_169:
    # Assignment: token_hint = ...

    # Arithmetic expression
    # Function call: peek_next_token_type()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call peek_next_token_type
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -88(%rbp)  # Store to token_hint

    # If statement

    # Comparison with logical chaining
    movq -88(%rbp), %r8  # Load token_hint
    movq $5, %r9  # Literal
    cmpq %r9, %r8
    movq $0, %rax
    sete %al
    # Single comparison result in rax
    test %rax, %rax
    jz .if_end_171
    # Then body
    # Assignment: ws_result = ...

    # Arithmetic expression
    # Function call: skip_line_comment()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call skip_line_comment
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -72(%rbp)  # Store to ws_result
    # Assignment: pos = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 0(%rbx), %rax  # Get element at index 0
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -16(%rbp)  # Store to pos
    # Assignment: col = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 8(%rbx), %rax  # Get element at index 1
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -32(%rbp)  # Store to col
    # Assignment: line = ...

    # Arithmetic expression
    # Array index access: ws_result
    movq -72(%rbp), %rbx  # Load array pointer from stack
    movq 16(%rbx), %rax  # Get element at index 2
    movq %rax, %r8  # Array element value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -24(%rbp)  # Store to line

    # Arithmetic expression
    # Function call: tokenize_next()
    movq -8(%rbp), %r10  # Load source
    pushq %r10  # Save arg 1
    movq -16(%rbp), %r10  # Load pos
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call tokenize_next
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Ltokenize_next_return
.if_end_171:
    # Assignment: token = ...

    # Arithmetic expression
    # Function call: create_token()
    movq $81, %r10  # Literal
    pushq %r10  # Save arg 1
    .section .rodata
    _str_2: .asciz "?"
    .section .text
    leaq _str_2(%rip), %r10  # Load string pointer
    pushq %r10  # Save arg 2
    movq -24(%rbp), %r10  # Load line
    pushq %r10  # Save arg 3
    movq -32(%rbp), %r10  # Load col
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_token
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -80(%rbp)  # Store to token

    # Arithmetic expression
    # Collection literal
    # Array literal: 4 elements, type=0
    # Allocate array on heap
    movq $4, %rdi  # Number of elements
    movq $8, %rsi   # Element size (8 bytes)
    call sto_array_alloc  # Returns pointer in rax
    movq %rax, %rbx  # Save array pointer in rbx
    # Initialize array elements
    # Element 0
    pushq %rbx

    # Arithmetic expression
    movq -80(%rbp), %r8  # Load token
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 0(%rbx)  # Store element at index 0
    # Element 1
    pushq %rbx

    # Arithmetic expression
    movq -16(%rbp), %r8  # Load pos
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_9
    jmp .no_overflow_9
.overflow_detected_9:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_9:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 8(%rbx)  # Store element at index 1
    # Element 2
    pushq %rbx

    # Arithmetic expression
    movq -32(%rbp), %r8  # Load col
    movq $1, %r9  # Literal
    addq %r9, %r8
    jo .overflow_detected_10
    jmp .no_overflow_10
.overflow_detected_10:
    # Overflow detected - promote to BigDecimal
    pushq %rdi
    pushq %rsi
    movq %r8, %rdi
    call sto_bigdec_from_int64
    movq %rax, %rdi # First BigDecimal
    movq %r9, %rsi
    call sto_bigdec_from_int64
    movq %rax, %rsi # Second BigDecimal
    call sto_bigdec_add
    popq %rsi
    popq %rdi
    movq %rax, %r8 # BigDecimal pointer in result register
.no_overflow_10:
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 16(%rbx)  # Store element at index 2
    # Element 3
    pushq %rbx

    # Arithmetic expression
    movq -24(%rbp), %r8  # Load line
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Move result to rax
    popq %rbx
    movq %rax, 24(%rbx)  # Store element at index 3
    movq %rbx, %rax  # Array pointer result
    movq %rax, %r8  # Collection pointer
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Ltokenize_next_return

.Ltokenize_next_return:
    movq %rbp, %rsp
    popq %rbp
    ret

# Function: main
.global main
main:
    pushq %rbp
    movq %rsp, %rbp
    subq $96, %rsp
    # Variable: test_source at -8(%rbp)

.section .rodata
.str_6:
    .string "function main()"
.text
    leaq .str_6(%rip), %r8  # Load string address
    movq %r8, -8(%rbp)  # Initialize test_source
    # Variable: pos at -16(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -16(%rbp)  # Initialize pos
    # Variable: line at -24(%rbp)
    movq $1, %r8  # Literal value
    movq %r8, -24(%rbp)  # Initialize line
    # Variable: col at -32(%rbp)
    movq $1, %r8  # Literal value
    movq %r8, -32(%rbp)  # Initialize col
    # Variable: new_pos at -40(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -40(%rbp)  # Initialize new_pos
    # Variable: new_col at -48(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -48(%rbp)  # Initialize new_col
    # Variable: new_line at -56(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -56(%rbp)  # Initialize new_line
    # Variable: token at -64(%rbp)

    # Arithmetic expression
    # Function call: create_token()
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 1
    .section .rodata
    _str_3: .asciz ""
    .section .text
    leaq _str_3(%rip), %r10  # Load string pointer
    pushq %r10  # Save arg 2
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 3
    movq $0, %r10  # Literal
    pushq %r10  # Save arg 4
    popq %rcx  # Restore arg 4
    popq %rdx  # Restore arg 3
    popq %rsi  # Restore arg 2
    popq %rdi  # Restore arg 1
    call create_token
    movq %rax, %r8  # Function return value
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, -64(%rbp)  # Initialize token
    # Variable: result_0 at -72(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -72(%rbp)  # Initialize result_0
    # Variable: result_1 at -80(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -80(%rbp)  # Initialize result_1
    # Variable: result_2 at -88(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -88(%rbp)  # Initialize result_2
    # Variable: result_3 at -96(%rbp)
    movq $0, %r8  # Literal value
    movq %r8, -96(%rbp)  # Initialize result_3

    # Arithmetic expression
    movq $0, %r8  # Literal
    # Result in r8 (integer) or xmm0 (float)
    movq %r8, %rax  # Return value
    jmp .Lmain_return

.Lmain_return:
    movq %rbp, %rsp
    popq %rbp
    ret
