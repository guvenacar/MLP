-- String utilities module

module StringUtils
    export func greet(text name) returns text
        return string_concat("Hello, ", name)
    end func
    
    export func shout(text msg) returns text
        return string_concat(msg, "!!!")
    end func
end module
