// ARITHMETIC MODULE TEST
// Tests all arithmetic module functions

fn main() {
    // Test arithmetic operations
    let add = 10 + 5;
    let sub = 20 - 8;
    let mul = 6 * 7;
    let div = 100 / 5;
    
    print_int(add);  // Should print 15
    print_int(sub);  // Should print 12
    print_int(mul);  // Should print 42
    print_int(div);  // Should print 20
    
    return 0;
}
