// MELP Variable Module Compiler - Simplified Version
// Just counts and returns, no print

numeric VAR_NUMERIC = 0
numeric VAR_STRING = 1
numeric VAR_BOOLEAN = 2

fn count_vars() -> numeric {
    numeric a = 1
    numeric b = 1
    numeric c = 1
    numeric ab = a + b
    numeric total = ab + c
    return total
}

fn main() -> numeric {
    numeric result = count_vars()
    return result
}
