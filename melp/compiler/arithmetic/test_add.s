# Generated by MELP Simple Codegen
.text

# Function: main
.globl main
.type main, @function
main:
    pushq %rbp
    movq %rsp, %rbp
    subq $128, %rsp  # allocate stack for locals
    movq $1, %rax
    movq %rax, -8(%rbp)  # store a
    movq $1, %rax
    movq %rax, -16(%rbp)  # store b
    movq $1, %rax
    movq %rax, -24(%rbp)  # store c
    movq -8(%rbp), %rax  # load a
    pushq %rax  # save left operand
    movq -16(%rbp), %rax  # load b
    movq %rax, %rbx  # right operand in rbx
    popq %rax  # restore left operand
    addq %rbx, %rax
    movq %rax, -32(%rbp)  # store total
    movq -32(%rbp), %rax  # load total
    # print_int(...)
    movq %rax, %rsi  # value to print
    leaq .str0(%rip), %rdi
    xorq %rax, %rax  # no floating point
    call printf@PLT
    movq $0, %rax
    # return expression
    leave
    ret
    # end of main
.size main, .-main

.section .rodata
.str0:
    .string "%ld\n"

.section .note.GNU-stack,"",@progbits
