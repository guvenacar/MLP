#define PI 3.14
#define VERSION "1.0"
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define DEBUG

#ifdef DEBUG
numeric x = PI
#else
numeric x = 0
#endif

#ifndef RELEASE
text msg = VERSION
#endif

#include "math.mlp"
#pragma once
