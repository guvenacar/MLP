#ifndef COMPARISON_PARSER_H
#define COMPARISON_PARSER_H

#include "comparison.h"
#include "../../parser.h"
#include "../arithmetic/arithmetic_parser.h"

typedef struct ComparisonExpr {
    Expression expr;      // Base expression (type should be EXPR_COMPARISON)
    Expression* left;
    Expression* right;
    ComparisonOp op;
} ComparisonExpr;

ComparisonExpr* comparison_parse(Parser* parser);
void comparison_free(ComparisonExpr* cmp);

#endif
