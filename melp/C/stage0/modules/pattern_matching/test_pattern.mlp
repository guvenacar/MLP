-- Pattern Matching Test
-- MLP syntax: -- comment

-- Variable declarations
numeric value = 42
text result = "unknown"

-- Pattern matching examples (comments for detection):
-- match value
--   1 => "one"
--   2..10 => "small"
--   x when x > 100 => "big"
--   _ => "default"
-- end match

numeric x = 100
