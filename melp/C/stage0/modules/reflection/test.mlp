@serializable
@deprecated("use NewPerson")
struct Person
    name: text
    age: numeric
    active: boolean
end struct

enum Color
    Red
    Green
    Blue
end enum

trait Drawable
    function draw
    function resize
end trait

function main
    numeric t = typeof(Person)
    text n = nameof(Person)
end function
