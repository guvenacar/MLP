#include "bitwise_operations_parser.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Parse bitwise operations
BitwiseExpr* parse_bitwise_operation(Lexer* lexer) {
    // TODO: Implement bitwise operation parsing
    // Bu fonksiyon şunları yapmalı:
    // 1. Token'ları okuyup bitwise operatörleri tanımla (&, |, ^, ~, <<, >>)
    // 2. BitwiseExpr yapısını doldur
    // 3. Precedence kurallarını uygula
    // 4. Nested expression'ları handle et

    fprintf(stderr, "Bitwise operations parsing not yet implemented\n");
    return NULL;
}
