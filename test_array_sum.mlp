function sum_array(arr[], size)
    numeric total = 0
    numeric i = 0
    while i < size
        total = total + arr[i]
        i = i + 1
    end
    return total
end

numeric numbers[] = [10, 20, 30, 40, 50]
numeric result = sum_array(numbers, 5)
print(result)
