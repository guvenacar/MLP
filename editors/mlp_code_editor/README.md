# MLP Code Editor Prototype

Standalone Monaco Editor integration for MLP, using shared styles.

## Setup
```bash
cd editors/mlp_code_editor
npm install
npm start
```

## Features
- Monaco Editor embedded in Electron
- Uses `shared/themes/variables.css` for UI styling
- Basic toolbar and layout
